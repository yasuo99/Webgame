@model IEnumerable<DichVuGame.Models.Code>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/Admin/Views/AdminHome/_CMSLayout.cshtml";
}

<h1>Index</h1>

<div class="row">
    <div class="col-6">Danh sách code game</div>
</div>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Game)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Gamecode)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Available)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Game.ID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Gamecode)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Available)
            </td>
            <td>
                <div class="btn-group">                   
                    <a type="button" class="btn btn-success" asp-action="Edit" asp-controller="Codes" asp-route-id="@item.ID"><i class="fas fa-edit"></i></a>
                    <a type="button" class="btn btn-info" asp-action="Details" asp-controller="Codes" asp-route-id="@item.ID"><i class="far fa-list-alt"></i></a>
                    <a type="button" class="btn btn-danger" asp-action="Delete" asp-controller="Codes" asp-route-id="@item.ID"><i class="fas fa-trash-alt"></i></a>
                </div>
            </td>
        </tr>
}
    </tbody>
</table>
